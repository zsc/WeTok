"""Utilities for WeTok.

This package intentionally keeps dependencies lightweight so that simple
inference scripts (e.g. `generate_wetok.py`) can run without installing the
full training stack.
"""

